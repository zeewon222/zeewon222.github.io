import { Data, Override } from "framer"

const data = Data({
    rotate: 0,
    rotateY: 0,
    toggle: true,
})

export function X(): Override {
    return {
        onItemTapped: (index, fields) => {
            console.log(index, fields)
        },
    }
}

export function onTap(): Override {
    return {
        onItemTapped: (index, fields) => {
            if (index === 0) {
                console.log("first item clicked")
            }
        },
    }
}
